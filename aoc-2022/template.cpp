#include <bits/stdc++.h>

#define NL '\n'

typedef long long ll;
typedef long unsigned int lui;

using namespace std;

int main(int argc, char* argv[]) {
	ifstream f(argv[1]);

	string line;
	while(getline(f, line)) {

	}
}
